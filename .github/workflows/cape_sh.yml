on:
  push:
    branch:
      - master

name: Test build of cape2.sh

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run script file
      run: |
         chmod a+x "${GITHUB_WORKSPACE}/installer/cape2.sh"
         "${GITHUB_WORKSPACE}/installer/cape2.sh" base
      shell: bash
