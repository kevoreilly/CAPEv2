# CAPEv2 汉化进度

更新时间：2025-11-15

## 已完成模块
- 登录/提交/概览/网络等主视图（含导航、仪表盘、分析页 Tab、通用按钮）。
- Analysis Generic 相关面板：VT、Yara/CAPE Yara、FLOSS、PE、DotNET、PDF、LNK、Office、RDP、XLM 宏等。
- 行为分析页面（树、进程列表、搜索/过滤）。
- 部分内存页入口：入口按钮、本页 Tab 标签、`_pslist` 表头。
- Compare/Debugger/MISP 视图：页面表头、按钮、空状态提示全部完成本地化。

## 待翻译模块及优先级
| 优先级 | 模块 | 说明 |
| --- | --- | --- |
| P0 | `analysis/memory/*` 其余模版（`_psscan`, `_rootkit`, `_svcscan`, `_malfind`, `_callbacks`, `_yarascan` 等） | 文本量大，直接面对蓝队/分析人员，需优先完成 |
| P1 | `analysis/network/*` 剩余子页面（HTTP/TLS/Suricata 详情、_sockscan/_netscan） | 当前仅部分 Tab 翻译；表头、提示仍为英文 |
| P1 | `analysis/compare/*`、`analysis/debugger/*`、`analysis/misp/*` | ✅ 2025-11-15 已完成；持续关注新增字段 |
| P2 | `analysis/overview/_info.html` 中日志/错误信息的细粒度字段 | 仍有英文提示，如 “Error loading data” 等 |
| P3 | 低频模版（`analysis/similar`, `analysis/tracee`, `analysis/dropped`, `analysis/procdump`, `analysis/procmemory` 等） | ✅ 2025-11-15 已完成；后续仅需跟进新增字段 |

## 当前进行中
- Memory 模块：已完成总览页 (index) 与 `_pslist`，剩余子模板待翻译。

> 备注：所有新译内容均需更新 `locale/zh_Hans/LC_MESSAGES/django.po` 并执行 `compilemessages`，以确保切换语言生效。
